index=your_index_name "Retrieving customer by"
		| rex "Retrieving customer by (?
<IDType>[A-Za-z]+) ID with \w+ ID (?
	<IDValue>[A-Za-z0-9]+) and status = (?
		<Status>[A-Z]+)"
			| stats count by IDType, IDValue, Status
